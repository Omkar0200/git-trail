git init

git add .

git status

git commit -m " "

git log

git diff "file name"

git checkout "file name"

git branch -M main

git remote add origin "repo url"

git push -u origin main

git remote -v

git remote set-url origin "url"

git rm --cached

git branch " name of the branch "

git branch 

git checkout " bname of the branch "

git merge " name of the branch "